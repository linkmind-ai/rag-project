from langchain_core.prompts import ChatPromptTemplate

_GET_EVIDENCE_PROMPT = ChatPromptTemplate.from_template("""
당신은 근거 문서 분석 전문가입니다. 생성된 답변이 실제로 어떤 문서를 참조했는지 정확히 식별해야 합니다.

## 분석 대상

### 사용자 질문
{query}

### 생성된 답변
{answer}

### 검색된 문서 목록
{documents}

## 분석 지침

**중요: 각 문서는 [인덱스 N] 형식으로 번호가 매겨져 있습니다. 반드시 이 인덱스 번호를 기준으로 판단하세요.**

다음 기준으로 근거 문서를 선별하세요:

1. **키워드 일치 분석**: 답변에 등장하는 핵심 개념/용어가 해당 문서에도 존재하는지 확인
   - 예: 답변에 "통제욕구"가 있다면, "통제욕구"를 언급한 문서가 근거 문서

2. **의미적 연관성**: 답변의 논지나 설명이 문서의 내용과 직접적으로 연결되는지 확인
   - 단순히 비슷한 주제가 아닌, 실제 답변 생성에 기여한 문서만 선택

3. **제외 기준**: 답변과 관련 없는 문서는 반드시 제외
   - 질문 주제와 다른 내용의 문서
   - 답변에서 언급되지 않은 개념만 포함된 문서

## 출력 형식

**반드시 아래 JSON 형식으로만 응답하세요. 다른 텍스트는 포함하지 마세요.**

{{"evidence_indices": [0, 2]}}

인덱스는 0부터 시작합니다. 근거 문서가 없으면 빈 배열 []을 반환하세요.
""")
